# ========================================
# ENVIRONMENT VARIABLES - NETLIFY FUNCTIONS
# ========================================
# Copy file ini ke .env untuk development lokal dengan Netlify CLI
# Untuk production, set di: Netlify Dashboard > Site Settings > Environment Variables

# Supabase Project URL
# Dapatkan dari: Supabase Dashboard > Settings > API
# Format: https://xxxxxxxxxxxxx.supabase.co
SUPABASE_URL=your_supabase_project_url

# Supabase Anon Key (Public Key)
# Dapatkan dari: Supabase Dashboard > Settings > API
# PENTING: Gunakan "anon public" key, BUKAN "service_role" key!
SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Gemini API Key (untuk Netlify Function `gemini.js`)
# Dapatkan dari: https://ai.google.dev/ (Get API key)
# Simpan sebagai environment variable (server-side), jangan di frontend.
GEMINI_API_KEY=your_gemini_api_key

# Netlify Site URL (auto-generated di production)
# Untuk development lokal, gunakan: http://localhost:8888
URL=http://localhost:8888

# CATATAN KEAMANAN:
# - API keys disimpan di Netlify Functions (server-side)
# - Frontend tidak mengandung kredensial Supabase
# - Operasi auth dilakukan lewat /.netlify/functions/auth
# - Jangan commit file .env ke git (sudah ada di .gitignore)
# - Jangan gunakan service_role key; gunakan anon key sesuai policy

# PANDUAN SINGKAT (Development Lokal):
# - Salin file ini menjadi .env dan isi nilai variabelnya
# - Jalankan: netlify dev (Netlify CLI)
# - Functions akan membaca variabel dari .env secara otomatis
